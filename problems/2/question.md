Write a function first_unique_window(nums, k) 
that returns the starting index of the first contiguous subarray of length k 
a) containing all consecutive values. 
b) containing all distinct values. 
Return -1 if no such window exists.
Aim for better than O(nk)

Write a function first_unique_window(nums, k) 
that returns the starting index of the first contiguous subarray of length k 
containing all consecutive values. 
Return -1 if no such window exists.
Aim for better than O(nk)



